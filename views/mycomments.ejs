<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f8f8f8;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        flex-direction: column;
    }
    h2 {
        color: #333;
    }
    p {
        color: #666;
        text-align: center;
    }
    form {
        margin-top: 10px;
        text-align: center;
    }
    input[type="text"][hidden] {
        display: none;
    }
    button[type="submit"] {
        background-color: #007bff;
        color: #fff;
        border: none;
        padding: 8px 16px;
        cursor: pointer;
        border-radius: 4px;
    }
    button[type="submit"]:hover {
        background-color: #0056b3;
    }
    a {
        color: #007bff;
        text-decoration: none;
        margin-top: 20px;
        display: block;
        width: fit-content;
        text-align: center;
    }
    a:hover {
        text-decoration: underline;
    }
</style>
<body>
    <% if(comments.length > 0) { %>
        <% comments.forEach(comment => { %>
            <h2><%= comment.heading %></h2>
        <p><%= comment.coment %></p>
        <form action="/deletecomment" method="post">
           
                <input type="text" hidden value="<%= comment.coment%>" name="commenttd" id="">
                <input type="text" hidden value="<%= comment.heading %>" name="commenthead" id="">
              <button type="submit"  >delete comment</button>
              
              </form>
              
        <% }); %>
   
  <% } else { %>
    <p>No comments found</p>
    <a href="/secrets">go to home</a>
  <% } %>
</body>
</html>